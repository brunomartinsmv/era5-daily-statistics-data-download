# Linux setup

Run the automated setup from the repository root:

```bash
bash scripts/setup_linux.sh
```

If you also need PDF compilation tools:

```bash
bash scripts/setup_linux.sh --with-latex
```
